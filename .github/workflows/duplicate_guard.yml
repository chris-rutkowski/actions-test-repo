name: Duplicate Guard
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  duplicate_guard:
    runs-on: ubuntu-latest
    steps:
      - name: Duplicate Guard
        uses: chris-rutkowski/duplicate-guard@v102
        # with:
        #   ignore_file: my_duplicate_guard.ignore
