name: Optim Guard
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  optim_guard:
    runs-on: ubuntu-latest
    steps:
      - name: Optim Guard
        uses: chris-rutkowski/optim-guard@v2.0.0
        # with:
        #   ignore_file: my_optim_guard.ignore
